# SPEC.md — 乃 -NANJI- サイト 確定仕様書

承認日: 2026-09-08（フェーズ2 承認済み・凍結）
作業ディレクトリ: `/Users/sugiyamahitoshihiko/Downloads/業務委託、就職/オレンダ、クロウド/nanji-site`
成果物: `preview/index.html` + `preview/style.css` + `preview/main.js`
プレビュー: https://nanji2025.com/preview/
本番（無傷）: https://nanji2025.com/

---

## 1. 参照サイト実測値（1440×900 で実測。二度と再解析しない）

### 1-1. Fondazione Prada — https://www.fondazioneprada.org

| 項目 | 実測値 |
|---|---|
| body 背景 | `rgb(255,255,255)` |
| body 文字 | `rgb(0,0,0)` |
| 本文色（最頻 1020回） | `rgb(20,20,20)` |
| muted（nav link, 171回） | `rgb(165,165,165)` |
| 罫線（107回） | `rgb(214,214,214)` |
| 影 | `rgba(0,0,0,0.05)` |
| 書体 | `StrongFontHeavy`(neuzeitsheavy-book) / `StrongFont`(neuzeits-book) = Neuzeit Grotesk |
| container | **1140px 固定**, padding `120px 10px 0` |
| header 高 | 86px, bg 白 |
| 見出しL | 100px / lh 90px / ls **-5.6px** / w400 / UPPERCASE |
| 見出しM | 60px / lh 55px / ls **-3.36px** / w400 / UPPERCASE |
| 見出しS | 40px / lh 35px / ls **-2.24px** / w400 / UPPERCASE |
| 小見出し | 26px / lh 26px / ls **-1.456px** / w400 / UPPERCASE |
| 本文 | 15px / lh 24px / ls +0.1px / `#141414` |
| nav link | 20.006px / UPPERCASE / `#A5A5A5` |
| 画像比 | **1.50 (3:2)** と **1.33 (4:3)** の2種のみ |
| 画像幅 | 835 / 550 / 265px（container 1140 に対し 73% / 48% / 23%） |
| モーション | `opacity 0.3s ease` / `opacity 0.4s ease-in-out` — **transform を一切使わない** |

> **決定的発見**：見出し4段すべて字間が `-0.056em` で完全に一致（100→-5.6 / 60→-3.36 / 40→-2.24 / 26→-1.456）。
> 行間は常に fontSize × 0.9。**単一の比率ルールを全段で反復**しているのが、このサイトの緊張感の正体。

### 1-2. M+ — https://www.mplus.org.hk/en/

| 項目 | 実測値 |
|---|---|
| 地 | `rgb(255,255,255)`（922回で圧倒的） |
| アクセント8色 | `#009EF2` 青24 / `#8F8AFA` 紫20 / `#FC578A` 桃13 / `#49A297` 青緑12 / `#FF5C0F` 橙8 / `#009EDE` 青2 7 / `#1AAB4A` 緑5 / `#C7872B` 黄土3 |
| 書体 | `Simplistic`（自社 Mplus family、幾何学グロテスク） |
| 見出しL | 128px / lh 120px / ls -5px / w400 |
| 見出しM | 48px / lh 48px / ls -1px / w400 |
| 見出しS | 40px / lh 40px / ls -1px / w400 |
| 中文見出し | 24px / lh 32px / ls -0.5px / **w500** |
| 本文・メタ | 16px / lh 24px / ls normal |
| ラベル | **11px / lh 16px / ls +0.5px / w500 / UPPERCASE** |
| 画像比 | ヒーロー 1.74 / **カード 1.71 (12:7)** / ワイド 2.06 |
| カード画像 | 480×280 |
| レイアウト | full-bleed 1440（container 制限なし） |
| 背景機構 | `PagesHomeBackground-block` が 900px × 8枚。スクロールで背景色が段階的に変わる |

**モーション（実測・最重要）**

| 用途 | 値 |
|---|---|
| **スクロールリビール** | `transform, opacity` / **0.85s** / **`cubic-bezier(0.165, 0.84, 0.44, 1)`** / delay **0 → 0.15 → 0.3 → 0.45 → 0.6s のスタガー** |
| 画像リビール | `clip-path` / 0.9s / 同イージング / delay 0.15s |
| hover | `transform` / 0.35s / `cubic-bezier(0.43, 0.42, 0.61, 1)` |
| 細かいUI | `transform` / 0.15s / ease |
| リンク下線 | `background-size, color` / 0.15s / ease（background-size で下線を伸ばす手法） |

> `cubic-bezier(0.165,0.84,0.44,1)` = easeOutQuart。**0.85s と長め＋0.15s刻みの遅延**で、要素が順に立ち上がる。これが「少し派手だが上品」の正体。

### 1-3. 取得不可（正直記載）
- M+ の折り返し以下のスクリーンショット。スクロール発火＋clip-path の描画が Browser pane で取得できず。
  → 代替として全ページテキスト・全computed style・全モーション値は取得済みのため、設計に支障なし。

---

## 2. 借りるもの / 借りないもの

| 参照元の要素 | 判定 | 理由 |
|---|---|---|
| 単一の字間比率を全見出し段で反復する**手法** | **借りる** | 手法。意匠ではない |
| Prada の `-0.056em` という**具体値** | **借りない** | 参照元の識別子そのもの。乃固有の値に翻訳 |
| Prada の `1140px` container | **借りない** | 同上。`1120px` に翻訳 |
| Neuzeit / Simplistic のフォント実体 | **借りない** | 著作物。分類（幾何学グロテスク）だけ借りる |
| Prada のモノクロ4階調という**構成** | **借りる** | 手法。具体値は自前に置換 |
| M+ の8色パレット具体値 | **借りない** | 参照元のブランド。乃固有の4色に翻訳 |
| M+ の「セクションで色が変わる」**手法** | **借りる** | 手法 |
| M+ のイージング関数・duration・stagger 間隔 | **借りる** | モーションの原理。数式は意匠ではない |
| M+ のヒーロー下端3色バー**構成** | **借りない** | 3本は乃の規模に合わない。2本に翻訳 |
| M+ の画像比 1.71 | **借りない** | 3:2 に翻訳 |
| 参照元の画像・文章・ロゴ | **借りない** | 著作物 |

---

## 3. 乃固有の事実への翻訳

| 設計原理（参照元） | 乃での翻訳 | 根拠となる乃固有の事実 |
|---|---|---|
| 単一の字間ルールを全見出しで反復（Prada） | 全見出しの字間を **`-0.04em`** に統一。見出しは **4段階のみ**（96/56/36/22px）、行間は常に fs×0.9 | **年4クール制**（各3ヶ月／2027年から）。段数=クール数、字間の 4 も同源 |
| セクションごとに色が変わる（M+） | 背景は白のまま。**アクセント4色**が Programme の各クールと展覧会ラベルに割り当たる | 伊賀焼の三様相＋隣接する湯（下記4色） |
| 高彩度色＝操作可能の記号（M+） | アクセント色は**会期・季節の識別にのみ使う**。装飾に使わない | 情報が主、装飾が従という乃の姿勢 |
| ヒーロー下端の3色CTAバー（M+） | **2本**に減らす：「展示を見る」「一乃湯へ」 | **乃と一乃湯が道を挟んで向かい合っている**という物理的事実 |
| 1140px の禁欲的 container（Prada） | **1120px** | 古民家の限られた間口。値は Prada から独立させるため別値 |
| opacity のみの静かなモーション（Prada） | 骨格は静かに保ち、**動きは M+ 側を採用**（ユーザー指定） | 指定通り |

### アクセント4色（伊賀焼の三様相＋湯）

| クール | 会期 | 色名 | 値 | 由来 |
|---|---|---|---|---|
| Cool 01 | 2–4月 | ビードロ | `#5E7A52` | 伊賀焼のビードロ釉（灰が溶けたガラス質の緑） |
| Cool 02 | 5–7月 | 湯 | `#6B8CA3` | 隣接する一乃湯。湯気と水の色 |
| Cool 03 | 8–10月 | 緋色 | `#A85434` | 伊賀焼の火色（窯の炎が当たった面） |
| Cool 04 | 11–1月 | 焦げ | `#3D342C` | 伊賀焼の焦げ（薪の灰が炭化した黒褐色） |

> 根拠：開廊記念展が「セラミカルパレード」、第2弾が「The Ceramical Parades ―やきもの中心主義―」であり、
> **やきものが乃の出自**。かつ一乃湯に隣接。この2つの事実だけで4色すべてを説明できる。

---

## 4. デザイントークン（確定値）

```
/* 地と墨 */
--paper:    #FFFFFF   地
--ink:      #000000   見出し
--ink-body: #1A1A1A   本文
--ink-mute: #8C8C8C   メタ・補助
--rule:     #DCDCDC   罫線
--wash:     #F6F6F4   面の切り替え（ごく薄い地）

/* アクセント（4クール／伊賀焼＋湯） */
--c01: #5E7A52  ビードロ
--c02: #6B8CA3  湯
--c03: #A85434  緋色
--c04: #3D342C  焦げ

/* 書体 */
--en: 'Archivo', system-ui, sans-serif        （幾何学グロテスク）
--jp: 'Zen Kaku Gothic New', sans-serif        （幾何学的な日本語ゴシック）

/* 見出し4段（字間はすべて -0.04em、行間はすべて fs×0.9） */
--h1: 96px / 86px
--h2: 56px / 50px
--h3: 36px / 32px
--h4: 22px / 20px
英字見出しは UPPERCASE、日本語は通常。

/* 本文・ラベル */
本文   15px / lh 26px / ls 0
小文字 13px / lh 22px
ラベル 11px / UPPERCASE / ls +0.08em / w600

/* レイアウト */
container 1120px
section padding 100px 24px（SP 64px 20px）
breakpoints 640 / 1024 / 1440

/* モーション（M+ 手法を借用） */
--ease-reveal: cubic-bezier(0.165, 0.84, 0.44, 1)
--ease-hover:  cubic-bezier(0.43, 0.42, 0.61, 1)
reveal: transform(translateY(28px)) + opacity, 0.85s, --ease-reveal, delay 0.15s刻み（最大5段）
image reveal: clip-path inset(0 0 100% 0) → inset(0), 0.9s, --ease-reveal, delay 0.15s
hover: transform 0.35s --ease-hover
link underline: background-size 0.15s ease

/* 画像比 */
hero 16:9 / card 3:2 / portrait 3:4
```

---

## 5. 情報設計とデータ入手元

| # | セクション | 実データの入手元 |
|---|---|---|
| 1 | Hero | 既存 `hero.JPG` ＋ 既存タグライン「伊賀のいとなみを、ものの温度へ。」 |
| 2 | 下端2バー | 「展示を見る」→ #now ／「一乃湯へ」→ yutonamisha.com |
| 3 | Now on View（展覧会） | `exhibition.html` の現行内容、`exhibition-2026-summer-flyer.jpg` 等 |
| 4 | Statement | 既存 index.html の杉山ディレクター文（全文流用） |
| 5 | Curator | note「ごあいさつ」＋既存文から構成。**顔写真は未支給**→ プレースホルダ |
| 6 | Programme | `opencall.html` の4クール定義（2027年2月〜2028年1月） |
| 7 | The Space | 既存 `photos/m-01〜05.jpg` `s-01〜03.jpg`（Photo: 三浦毬／杉山仁彦） |
| 8 | Archive | 3展（2025.11-12 / 2026.01-03 / 2026.03-05）＋ archive-*.jpg |
| 9 | Journal | note.com/nanji2025 の記事5本（タイトル実データ取得済み） |
| 10 | Neighborhood | 一乃湯（ゆとなみ社）／ Google Maps セレクトリスト |
| 11 | Store | BASE 商品ID（GitHub Actions が1時間毎に自動同期） |
| 12 | Visit | 住所 三重県伊賀市上野西日南町1762 ／ 14:00–23:00 木曜定休 ／ 入場無料 |

**未確定（要支給）**
- キュレーター顔写真（現在 `photos/s-01.jpg` を代用）
- フィルム写真（撮影者名が後日確定するまで不使用）
- 現在開催中の展覧会の正確な会期日付（「2026年秋」表記で暫定）

---

## 6. このサイトの良さが宿っている設計判断（言語化）

1. **Prada：単一の比率を全段で反復する。** 字間 -0.056em・行間 fs×0.9 を4段すべてで守り抜く。これにより、サイズが変わっても文字の「詰まり方」の印象が変わらない。→ 乃では -0.04em / fs×0.9 で反復。
2. **Prada：色を4階調に絞り、面で語らない。** 背景色は白のみ。区切りは罫線1本。→ 乃も踏襲。面の切り替えは `--wash` の一段だけに限定。
3. **M+：0.85s + 0.15s刻みの遅延で、要素を順に立ち上げる。** 一斉ではなく順序があることで、読む順番が身体に伝わる。→ 乃では見出し→本文→画像の順に遅延。
4. **M+：色は識別子であって装飾ではない。** 各セクションに固有色を与え、色を見れば現在地が分かる。→ 乃では4色をクール識別に限定使用。
5. **両者共通：画像の比率を2種類に固定する。** 比率が揺れないことでページ全体のリズムが安定する。→ 乃では 3:2（カード）と 3:4（人物）の2種のみ。

---

## 7. 実装方針

- ビルド不要の静的 HTML/CSS/JS
- ファイル分割：`index.html` / `style.css?v=N` / `main.js?v=N`（キャッシュ対策でクエリ番号を上げる）
- hover は必ず `@media (any-hover: hover)` で囲う
- `.preload * { transition:none !important }` を入れ、DOMContentLoaded で外す
- 画像未入稿でも成立するプレースホルダ
- コメントは日本語で「なぜ」を書く
